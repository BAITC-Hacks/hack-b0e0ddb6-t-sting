# PostgreSQL credentials for local development. Set a local password in .env.
POSTGRES_USER=app
POSTGRES_PASSWORD=local-development-only
POSTGRES_DB=app
# Database host and published port for the local API.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
# API listening port.
API_PORT=3000
